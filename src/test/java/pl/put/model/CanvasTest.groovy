package pl.put.model

import org.opencv.core.Core
import org.opencv.core.Mat
import org.opencv.core.Size
import spock.lang.Specification

class CanvasTest extends Specification {
//    def "creating new history"() {
//        when:
//        System.loadLibrary(Core.NATIVE_LIBRARY_NAME)
//        Mat mat = new Mat(new Size(0, 0), 0)
//        Canvas canvas = new Canvas(mat)
//
//        then:
//        canvas.isHistoryEmpty() == true
//    }
//
//    def "Adding to history"() {
//        when:
//        Mat actual = new Mat(new Size(0, 0), 0)
//        Canvas canvas = new Canvas(actual)
//        Mat historical = new Mat(new Size(10, 10), 0)
//        canvas.addToHistory(historical)
//
//        then:
//        canvas.isHistoryEmpty() == false
//        canvas.canvasHistory.last == historical
//        canvas.actualImage == actual
//    }
//
//    def "Update history"() {
//        when:
//        Mat actual = new Mat(new Size(0, 0), 0)
//        Canvas canvas = new Canvas(actual)
//        Mat historical = new Mat(new Size(10, 10), 0)
//        canvas.updateHistory(historical)
//
//        then:
//        canvas.isHistoryEmpty() == false
//        canvas.canvasHistory.last == actual
//        canvas.actualImage == historical
//    }
//
//    def "Undo" () {
//        when:
//        Mat actual = new Mat(new Size(0, 0))
//        Canvas canvas = new Canvas(actual)
//        Mat historical = new Mat(new Size(10, 10))
//        canvas.updateHistory(historical)
//
//    }
}
